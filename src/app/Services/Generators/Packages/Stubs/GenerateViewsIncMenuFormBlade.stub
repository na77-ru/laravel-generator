<div class="row justify-content-center">
    <div class="col-md-12">
        <div class="card">
            <form method="POST" action="{{ route('{{package_name}}_patch') }}">
                @method('PATCH')
                @csrf
                <div class="card-body">
                    <button type="submit"
                            class="btn btn-success">{{__('{{vendor-}}-{{package-name}}')}}</button>
                </div>
            </form>
        </div>
    </div>
</div>
