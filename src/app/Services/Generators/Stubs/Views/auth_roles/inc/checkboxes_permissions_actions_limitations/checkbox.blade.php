 {{--                        <label>{{__('try')}}</label>--}}
                <div class="checkbox">
                    <label>
                        <input type="hidden" name="{{ 'try' }}" value="0">
                        <input type="checkbox" value="{{ 1 }}"
                               name="{{ 'try' }}"
                               checked="checked"
                        >
                        {{ 'try' }}
                    </label>
                </div>
